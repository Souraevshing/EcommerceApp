spring:
  application:
    name: product
  config:
    import: optional:configserver:http://localhost:8888

  profiles:
    active: dev

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest