spring:
  application:
    name: configserver
  cloud:
    config:
      uri: http://localhost:8881
      fail-fast: true
    bootstrap:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: refresh
